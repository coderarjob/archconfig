source ~/.vimrc

" The reason to have GuiFont here is because if placed in .vimrc or init.vim,
" we get an error ( GuiFont is not an editor command )
" the ! character if not used nvim gives an error message.
" GuiFont! Monaco:h10.6
" GuiFont Inconsolata:h12
GuiFont Consolas:h11

